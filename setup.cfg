[metadata]
name = neurodamus
summary = Blue Brain Project - Neurodamus simulation control, now in Python
author = Fernando Pereira
author-email = fernando.pereira@epfl.ch
license = Blue Brain Project, EPFL Switzerland
home-page = http://...
description-file = README.rst
# Add here all kinds of additional classifiers as defined under
# https://pypi.python.org/pypi?%3Aaction=list_classifiers
classifier =
    Development Status :: 4 - Beta
    Programming Language :: Python

[test]
# py.test options when running `python setup.py test`
addopts = tests

[tool:pytest]
# Options for py.test:
# Specify command line options as you would do when invoking py.test directly.
# e.g. --cov-report html (or xml) for html/xml output or --junitxml junit.xml
# in order to write a coverage file that can be read by Jenkins.
addopts =
    tests
;    examples/composed_signal.py
;    examples/create_cell.py
;    examples/neuron_tut1.py
;    examples/neuron_tut2.py
;    examples/neuron_first_steps.py
;    --verbose
    # --cov pyneurodamus --cov-report term-missing

[aliases]
docs = build_sphinx

[bdist_wheel]
# Use this option if your package is pure-python
universal = 1

[build_sphinx]
source_dir = docs
build_dir = docs/_build

[pbr]
# Let pbr run sphinx-apidoc
autodoc_tree_index_modules = True
# autodoc_tree_excludes = ...
# Let pbr itself generate the apidoc
# autodoc_index_modules = True
# autodoc_exclude_modules = ...
# Convert warnings to errors
# warnerrors = True

[devpi:upload]
# Options for the devpi: PyPI server and packaging tool
# VCS export must be deactivated since we are using setuptools-scm
no-vcs = 1
formats = bdist_wheel

[flake8]
exclude = venv, docs
ignore = E221
max-line-length = 100

